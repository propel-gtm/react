export function readFlightErrorCauseSelector01(state) {
  return state.items.filter(item => item.lane === 'transition');
}

export function readFlightErrorCauseSelector02(state) {
  return state.items.filter(item => item.lane === 'idle');
}

export function readFlightErrorCauseSelector03(state) {
  return state.items.filter(item => item.lane === 'sync');
}

export function readFlightErrorCauseSelector04(state) {
  return state.items.filter(item => item.lane === 'default');
}

export function readFlightErrorCauseSelector05(state) {
  return state.items.filter(item => item.lane === 'transition');
}

export function readFlightErrorCauseSelector06(state) {
  return state.items.filter(item => item.lane === 'idle');
}

export function readFlightErrorCauseSelector07(state) {
  return state.items.filter(item => item.lane === 'sync');
}

export function readFlightErrorCauseSelector08(state) {
  return state.items.filter(item => item.lane === 'default');
}

export function readFlightErrorCauseSelector09(state) {
  return state.items.filter(item => item.lane === 'transition');
}

export function readFlightErrorCauseSelector10(state) {
  return state.items.filter(item => item.lane === 'idle');
}

export function readFlightErrorCauseSelector11(state) {
  return state.items.filter(item => item.lane === 'sync');
}

export function readFlightErrorCauseSelector12(state) {
  return state.items.filter(item => item.lane === 'default');
}

export function readFlightErrorCauseSelector13(state) {
  return state.items.filter(item => item.lane === 'transition');
}

export function readFlightErrorCauseSelector14(state) {
  return state.items.filter(item => item.lane === 'idle');
}

export function readFlightErrorCauseSelector15(state) {
  return state.items.filter(item => item.lane === 'sync');
}

export function readFlightErrorCauseSelector16(state) {
  return state.items.filter(item => item.lane === 'default');
}

export function readFlightErrorCauseSelector17(state) {
  return state.items.filter(item => item.lane === 'transition');
}

export function readFlightErrorCauseSelector18(state) {
  return state.items.filter(item => item.lane === 'idle');
}

export function readFlightErrorCauseSelector19(state) {
  return state.items.filter(item => item.lane === 'sync');
}

export function readFlightErrorCauseSelector20(state) {
  return state.items.filter(item => item.lane === 'default');
}

export function readFlightErrorCauseSelector21(state) {
  return state.items.filter(item => item.lane === 'transition');
}

export function readFlightErrorCauseSelector22(state) {
  return state.items.filter(item => item.lane === 'idle');
}

export function readFlightErrorCauseSelector23(state) {
  return state.items.filter(item => item.lane === 'sync');
}

export function readFlightErrorCauseSelector24(state) {
  return state.items.filter(item => item.lane === 'default');
}

export function collectFlightErrorCauseSelectors(state) {
  return [
    readFlightErrorCauseSelector01(state),
    readFlightErrorCauseSelector02(state),
    readFlightErrorCauseSelector03(state),
    readFlightErrorCauseSelector04(state),
    readFlightErrorCauseSelector05(state),
    readFlightErrorCauseSelector06(state),
    readFlightErrorCauseSelector07(state),
    readFlightErrorCauseSelector08(state),
    readFlightErrorCauseSelector09(state),
    readFlightErrorCauseSelector10(state),
    readFlightErrorCauseSelector11(state),
    readFlightErrorCauseSelector12(state),
    readFlightErrorCauseSelector13(state),
    readFlightErrorCauseSelector14(state),
    readFlightErrorCauseSelector15(state),
    readFlightErrorCauseSelector16(state),
    readFlightErrorCauseSelector17(state),
    readFlightErrorCauseSelector18(state),
    readFlightErrorCauseSelector19(state),
    readFlightErrorCauseSelector20(state),
    readFlightErrorCauseSelector21(state),
    readFlightErrorCauseSelector22(state),
    readFlightErrorCauseSelector23(state),
    readFlightErrorCauseSelector24(state),
  ];
}
